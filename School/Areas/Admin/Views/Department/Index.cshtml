@using School.Entities
@model List<Department>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="~/css/department.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body>
    <div class="main">
        <div class="container">
            <h2>Department List</h2>
            <div class="table-actions">
                <input type="text" id="search-bar" placeholder="Search by name..." />
            </div>
            <table class="table">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Department Name</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var obj in Model)
                    {
                        <tr onclick="window.location='@Url.Action("Info", "Department", new { id = obj.DepartmentId, area = "Admin" })'" style="cursor: pointer;">
                            <td>@obj.DepartmentId</td>
                            <td>@obj.Name</td>
                            <td><a asp-area="Admin" asp-controller="Department" asp-action="Edit" asp-route-id="@obj.DepartmentId" class="btn btn-warning">Update</a></td>
                            <td><a asp-area="Admin" asp-controller="Department" asp-action="Delete" asp-route-id="@obj.DepartmentId" class="btn btn-danger">Delete</a></td>
                        </tr>
                    }
                <tbody>
            </table>
        </div>
    </div>
    <script src="~/js/searchbar.js"></script>
</body>
</html>


